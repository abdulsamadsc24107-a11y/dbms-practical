CREATE TABLE Customer(CustID INT PRIMARY KEY, Name VARCHAR(50), Cust_Address VARCHAR(100), Phone_no VARCHAR(15), Email_Address VARCHAR(50), Age INT);
CREATE TABLE Branch(Branch_ID INT PRIMARY KEY, Branch_Name VARCHAR(50), Address VARCHAR(100));
CREATE TABLE Account(Account_no INT PRIMARY KEY, Branch_ID INT, CustID INT, date_open DATE, Account_type VARCHAR(20), Balance DECIMAL(10,2), FOREIGN KEY (Branch_ID) REFERENCES Branch(Branch_ID), FOREIGN KEY (CustID) REFERENCES Customer(CustID));

INSERT INTO Customer VALUES (101,'Amit','Pune','9876543210','amit@gmail.com',30);
INSERT INTO Customer VALUES (102,'Rahul','Mumbai','9123456780','rahul@gmail.com',45);
INSERT INTO Customer VALUES (103,'Sneha','Pune','9988776655','sneha@gmail.com',38);

INSERT INTO Branch VALUES (1,'Main Branch','Pune');
INSERT INTO Branch VALUES (2,'City Branch','Mumbai');

INSERT INTO Account VALUES (1001,1,101,'2018-08-16','Saving Account',50000.00);
INSERT INTO Account VALUES (1002,2,102,'2018-02-16','Saving Account',60000.00);
INSERT INTO Account VALUES (1003,1,103,'2018-08-16','Loan Account',30000.00);

ALTER TABLE Customer MODIFY Email_Address VARCHAR(20);
ALTER TABLE Customer MODIFY Email_Address VARCHAR(20) NOT NULL;

SELECT COUNT(*) FROM Account WHERE Balance > 50000;

SELECT AVG(Balance) FROM Account WHERE Account_type='Saving Account';

SELECT * FROM Customer WHERE Cust_Address='Pune' OR Name LIKE 'A%';

CREATE TABLE Saving_Account AS SELECT Account_no, Branch_ID, CustID, date_open, Balance FROM Account;

SELECT c.CustID, c.Name, c.Age, a.Balance 
FROM Customer c 
JOIN Account a ON c.CustID=a.CustID 
WHERE a.Balance >= 20000 
ORDER BY c.Age;
